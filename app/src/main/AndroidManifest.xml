<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.xxhong.rosclient">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".RCApplication"
        android:allowBackup="true"
        android:icon="@drawable/mg_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.PublishActivity" />
        <activity android:name=".ui.NodesActivity" />
        <activity android:name=".ui.DetailActivity"/>
        <activity android:name=".ui.Select"/>
        <activity android:name=".ui.Call"/>
        <activity android:name=".ui.PayActivity"/>
        <activity android:name=".ui.PathSelect"/>
        <activity android:name=".ui.Register"/>
    </application>

</manifest>